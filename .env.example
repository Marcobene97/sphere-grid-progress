# Supabase Configuration
# These are public keys and safe to include in your frontend build
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# OpenAI API Key for AI features (optional for local development)
# In production, store this in Supabase Edge Function secrets or Vercel environment variables
VITE_OPENAI_API_KEY=your-openai-key-here

# Do NOT include private keys like service role keys in this file
# Private keys should be set in your deployment environment or Supabase Edge Functions secrets